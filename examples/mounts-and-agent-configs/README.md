# Mounts and agent configs example

Shows host mounts plus agent config mounting.

Before running, create the referenced host directories if they do not exist:

```sh
mkdir -p shared-cache readonly-fixtures
```

Then run:

```sh
qemu-sandbox
```
